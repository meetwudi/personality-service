Keep the top-level folder structure to two folders only: `app/` and `harness/`.

Only change this top-level structure when a human explicitly requests it.

Always capture what was discussed, decided, and learned from the human under `harness/raw-outputs/`.
