This folder stores raw outputs, human decisions, and learned preferences.

Record entries in sequential order under `entries/` using zero-padded numeric prefixes, for example `0001-top-level-structure.md`.

Also maintain topic indexes under `topics/` so related entries can be found by subject.

Do not invent decisions, preferences, requirements, or acceptance criteria. Only record what the human explicitly said or what was directly learned from the conversation.
