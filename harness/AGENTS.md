This folder contains harness engineering assets for the personality service.

Follow the top-level structure rule: do not add new top-level folders unless a human explicitly requests it.

Keep requirements under `requirements/`, with modular requirement documents instead of large catch-all specs.

Keep raw outputs, human decisions, and learned preferences under `raw-outputs/`.
